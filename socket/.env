# --- WebSocket runtime (dedicated range, separate from RTP) ---

WS_BIND_HOST=0.0.0.0       # where the WS server binds (inside process/container)
WS_PUBLIC_HOST=127.0.0.1   # what clients (LLM/UI) should connect to (use your host/LAN IP if needed)
WS_PORT_MIN=12100
WS_PORT_MAX=13000

# POSTGRES CREDS
PG_HOST=192.168.14.12
PG_PORT=8063
PG_DATABASE=postgres
PG_USER=postgres
PG_PASSWORD=postgresadmin

# COMMANDS

# uvicorn main:api --reload --host 127.0.0.1 --port 8002
# docker build -t socket-service:1.0 -f socket/Dockerfile .
# docker run --name socket-service \
#   --env-file socket/.env \
#   -e WS_HOST=<PUBLIC_OR_HOST_IP> \
#   -p 8002:8002 \
#   -p 12100-13000:12100-13000 \
#   --restart unless-stopped \
#   socket-service:1.0